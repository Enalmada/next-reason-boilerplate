// @flow

/*
type Props = {
    children: React.Node
}
*/
// import "../../assets/styles.less";
/* eslint global-require: 0 */
// if (process.env.NODE_ENV === "development") {
//    require("../../assets/antd.less");
// }
// import "../assets/styles.less";
// require("../../assets/styles.less");

const App = ({children}) => (
    <main>
        {children}
    </main>
);

export default App;
